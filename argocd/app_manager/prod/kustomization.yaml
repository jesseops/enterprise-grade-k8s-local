apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../base

patches:
  - target:
      kind: Application
      name: argo-app-manager
    path: ./overlays/app-manager.yaml
  - target:
      kind: Application
    path: ./overlays/cluster-application.yaml
